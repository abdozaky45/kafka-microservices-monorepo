{
  "name": "user-system",
  "version": "1.0.0",
  "main": "index.js",
   "scripts": {
    "build": "npx tsc",
    "start": "node dist/server.js",
    "dev": "nodemon src/server.ts",
    "test": "npm run build && jasmine"
  }
}
